{
  "name": "live-calcio-admin-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-frontend": "cd frontend && npm run dev",
    "start-backend": "cd backend && nodemon server",
    "start": "concurrently \"npm run start-frontend\" \"npm run start-backend\"",
    "build": "npm install && cd frontend && npm install && cd .. && cd backend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
